version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: sugarstory-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: adminpassword
    volumes:
      - mongodata:/data/db

volumes:
  mongodata:
